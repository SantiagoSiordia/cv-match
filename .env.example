# Optional: persistent data directory (Docker/EFS). Defaults to cwd.
# CV_MATCH_DATA_ROOT=/data

# AI routing: auto (default) = try Amazon Bedrock first, then Google Gemini if Bedrock fails or AWS region is unset.
# Force one provider: bedrock | gemini
# AI_PROVIDER=auto

# Google Gemini (fallback in auto mode, or set AI_PROVIDER=gemini for local dev without AWS keys)
# GEMINI_API_KEY=
# Retries with backoff for 503/429 etc. (default 5)
# GEMINI_MAX_RETRIES=5
# Cheapest defaults: gemini-2.5-flash-lite (text), gemini-embedding-001 (embeddings)
# GEMINI_TEXT_MODEL=gemini-2.5-flash-lite
# GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# Amazon Bedrock (preferred when AWS_REGION is set and credentials work; IAM role on AWS or local AWS credentials)
# AWS_REGION=us-east-1
# BEDROCK_TEXT_MODEL_ID=anthropic.claude-3-5-haiku-20241022-v1:0
# BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0

# Bulk / evaluate throughput (optional)
# EVALUATE_CV_CONCURRENCY=3        — parallel compatibility LLM calls per job (non-batch mode)
# BULK_JOB_CONCURRENCY=2         — parallel jobs during bulk evaluate
# BULK_LLM_GLOBAL_CONCURRENCY=4  — max in-flight compatibility LLM calls across all bulk jobs; set 0 to disable
# LLM_THROTTLE_MAX_RETRIES=6     — retries with backoff on rate limits (Bedrock path; Gemini has its own retries)
# EVALUATE_LOG_TIMING=1          — JSON lines to stdout: matrix, per-CV, per-job timings

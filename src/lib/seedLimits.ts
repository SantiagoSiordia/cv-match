/** Default number of GitHub CV PDFs to ingest per full seed. */
export const SEED_DEFAULT_MAX_CV_FILES = 1500;

/** Hard upper bound for `maxCvFiles` (API / UI / CLI). */
export const SEED_MAX_CV_FILES_CAP = 5000;
